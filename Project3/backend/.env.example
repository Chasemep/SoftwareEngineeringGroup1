# Backend environment variables — copy to .env for local development
# These are overridden by docker-compose.yml environment section in Docker

PORT=3001
FRONTEND_URL=http://localhost:3000

# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_NAME=pnw_events
DB_USER=pnwuser
DB_PASSWORD=pnwpass
